# Example Prompts for `recursion-induction-foundations`

- "Use `recursion-induction-foundations` for this task and follow its workflow exactly."
- "Apply `recursion-induction-foundations` and return findings first, then implementation steps."
- "Run `recursion-induction-foundations` with strict validation and call out residual risks."
