# Example Prompts for `algorithm-correctness-invariants`

- "Use `algorithm-correctness-invariants` for this task and follow its workflow exactly."
- "Apply `algorithm-correctness-invariants` and return findings first, then implementation steps."
- "Run `algorithm-correctness-invariants` with strict validation and call out residual risks."
