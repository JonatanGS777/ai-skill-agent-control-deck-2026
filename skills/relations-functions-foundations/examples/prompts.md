# Example Prompts for `relations-functions-foundations`

- "Use `relations-functions-foundations` for this task and follow its workflow exactly."
- "Apply `relations-functions-foundations` and return findings first, then implementation steps."
- "Run `relations-functions-foundations` with strict validation and call out residual risks."
