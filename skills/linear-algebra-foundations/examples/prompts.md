# Example Prompts for `linear-algebra-foundations`

- "Use `linear-algebra-foundations` for this task and follow its workflow exactly."
- "Apply `linear-algebra-foundations` and return findings first, then implementation steps."
- "Run `linear-algebra-foundations` with strict validation and call out residual risks."
