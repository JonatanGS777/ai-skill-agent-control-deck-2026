# Example Prompts for `logic-of-programs-hoare`

- "Use `logic-of-programs-hoare` for this task and follow its workflow exactly."
- "Apply `logic-of-programs-hoare` and return findings first, then implementation steps."
- "Run `logic-of-programs-hoare` with strict validation and call out residual risks."
