# Example Prompts for `computability-decidability`

- "Use `computability-decidability` for this task and follow its workflow exactly."
- "Apply `computability-decidability` and return findings first, then implementation steps."
- "Run `computability-decidability` with strict validation and call out residual risks."
