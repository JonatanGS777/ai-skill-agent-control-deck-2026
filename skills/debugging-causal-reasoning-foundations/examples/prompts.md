# Example Prompts for `debugging-causal-reasoning-foundations`

- "Use `debugging-causal-reasoning-foundations` for this task and follow its workflow exactly."
- "Apply `debugging-causal-reasoning-foundations` and return findings first, then implementation steps."
- "Run `debugging-causal-reasoning-foundations` with strict validation and call out residual risks."
