# Example Prompts for `database-theory-normalization`

- "Use `database-theory-normalization` for this task and follow its workflow exactly."
- "Apply `database-theory-normalization` and return findings first, then implementation steps."
- "Run `database-theory-normalization` with strict validation and call out residual risks."
