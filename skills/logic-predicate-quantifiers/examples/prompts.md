# Example Prompts for `logic-predicate-quantifiers`

- "Use `logic-predicate-quantifiers` for this task and follow its workflow exactly."
- "Apply `logic-predicate-quantifiers` and return findings first, then implementation steps."
- "Run `logic-predicate-quantifiers` with strict validation and call out residual risks."
